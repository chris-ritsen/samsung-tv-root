name: build

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"
  UV_VERSION: "0.12.1"

jobs:
  test:
    name: test (${{ matrix.os }})
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv sync --locked --all-groups
      - run: uv run ruff check src tests tools swu
      - run: uv run pytest
      - run: uv run python tools/audit_release.py
      - run: uv build
      - run: uv run samsung-tv-root --version
      - run: uv run samsung-tv-root doctor --json

  payloads:
    name: build TV payloads
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68
        with:
          dotnet-version: "6.0.x"
      - name: Install ARM linker
        run: sudo apt-get update && sudo apt-get install --yes clang lld
      - run: make payloads swu-preloads
      - name: Verify payload types
        run: |
          for assembly in \
            payloads/qn90b/out/FdetProbe.dll \
            payloads/qn90b/out/SamsungTvRootAgent.dll \
            payloads/qn90b/out/SamsungTvRemoteInputAgent.dll \
            payloads/qn90b/out/Qn90bSourceControl.dll \
            payloads/qn90f/out/MaliPhysicalProbe.dll \
            payloads/qn90f/out/SamsungTvRootAgent.dll \
            payloads/qn90f/out/SamsungTvRemoteInputAgent.dll \
            payloads/qn90f/out/Qn90fSourceControl.dll \
            payloads/qn90f/out/Qn90fDisplayControl.dll; do
            file "$assembly" | tee /dev/stderr | grep -q 'Mono/.Net assembly'
          done
          for preload in swu/out/*.so; do
            file "$preload" | tee /dev/stderr | grep -q 'ELF 32-bit LSB shared object, ARM'
          done
          python -m json.tool payloads/qn90b/out/FdetProbe.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90b/out/SamsungTvRootAgent.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90b/out/SamsungTvRemoteInputAgent.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90b/out/Qn90bSourceControl.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90f/out/MaliPhysicalProbe.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90f/out/SamsungTvRootAgent.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90f/out/SamsungTvRemoteInputAgent.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90f/out/Qn90fSourceControl.runtimeconfig.json >/dev/null
          python -m json.tool payloads/qn90f/out/Qn90fDisplayControl.runtimeconfig.json >/dev/null
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: tv-payloads
          path: |
            payloads/qn90b/out
            payloads/qn90f/out
            swu/out
          if-no-files-found: error

  package:
    name: package (${{ matrix.os }})
    needs: [test, payloads]
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - ubuntu-24.04-arm
          - macos-15-intel
          - macos-15
          - windows-latest
          - windows-11-arm
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          version: ${{ env.UV_VERSION }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: tv-payloads
          path: .
      - run: uv sync --locked --all-groups
      - run: uv run python tools/build_release.py
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: release-${{ runner.os }}-${{ runner.arch }}
          path: |
            dist/releases/*.zip
            dist/releases/*.tar.gz
            dist/releases/*.sha256
          if-no-files-found: error

  release:
    name: publish GitHub release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: package
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          pattern: release-*
          path: dist/release-assets
          merge-multiple: true
      - name: Verify version and checksums
        run: |
          test "${GITHUB_REF_NAME}" = "v$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')"
          python tools/verify_release_assets.py dist/release-assets
      - name: Publish assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1 || \
            gh release create "${GITHUB_REF_NAME}" --generate-notes --verify-tag
          gh release upload "${GITHUB_REF_NAME}" \
            dist/release-assets/*.tar.gz \
            dist/release-assets/*.zip \
            --clobber
